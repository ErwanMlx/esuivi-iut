{% extends 'base.html.twig' %}

{% block title %}Ajout de compte{% endblock %}

{#{% block user %}Test{% endblock %}#}

{% block page_title %}Ajout de compte{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="text-center contenu">
                <a class="btn btn-default" href="{{ path('ajout_compte', {'type': "apprenti"}) }}">Ajouter un Apprenti</a>
                <a class="btn btn-default" href="{{ path('ajout_compte', {'type': "cfa"}) }}">Ajouter un responsable CFA</a>
                {% if is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-default" href="{{ path('ajout_compte', {'type': "iut"}) }}">Ajouter un responsable IUT</a>
                {% endif %}
                <a class="btn btn-default" href="{{ path('fos_user_change_password') }}">Modification mot de passe</a>
            </div>
        </div>
    </div>
{% endblock %}